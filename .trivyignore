# Suppressed until a patched package reaches the Debian release used by python:*-slim.
# CVE-2026-7598: libssh2 integer overflow in userauth (Debian tracker: fix in sid 1.11.1-3; trixie still on 1.11.1-1).
# Stratum runs uvicorn only; the image does not expose an SSH client that accepts attacker-controlled libssh2 auth to arbitrary hosts.
CVE-2026-7598
