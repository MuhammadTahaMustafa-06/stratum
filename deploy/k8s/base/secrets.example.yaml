# Template only — copy to secrets.yaml (gitignored) or create via kubectl / External Secrets.
# kubectl apply -f secrets.yaml -n stratum
apiVersion: v1
kind: Secret
metadata:
  name: stratum-backend-secrets
type: Opaque
stringData:
  JWT_SECRET_KEY: "replace-with-openssl-rand-hex-32"
  GROQ_API_KEY: "replace"
  DATABASE_URL: "postgresql+psycopg2://user:pass@host.neon.tech/neondb?sslmode=require"
